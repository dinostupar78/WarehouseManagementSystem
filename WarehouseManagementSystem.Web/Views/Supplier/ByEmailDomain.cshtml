@model IReadOnlyList<WarehouseManagementSystem.Model.Supplier>

@{
    ViewData["Title"] = "Suppliers by Email Domain";
}

<div class="wms-breadcrumbs">
    <a asp-controller="Home" asp-action="Index">Dashboard</a> &gt;
    <a asp-action="Index">SUPPLIERS</a> &gt;
    EMAIL DOMAIN
</div>

<h1 class="page-title">Suppliers using @ViewBag.EmailDomain</h1>

<div class="wms-table-container mt-4">
    <table class="table wms-table table-hover">
        <thead>
            <tr>
                <th>Registration ID</th>
                <th>Supplier Name</th>
                <th>Primary Contact</th>
                <th>Email Address</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <partial name="_SupplierListPartial" model="Model" />
        </tbody>
    </table>
</div>

<div class="mt-4">
    <a asp-action="Index" class="btn btn-outline-secondary px-4 fw-bold rounded-pill">
        &larr; Back to Suppliers
    </a>
</div>