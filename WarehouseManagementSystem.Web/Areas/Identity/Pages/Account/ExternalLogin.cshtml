@page
@model ExternalLoginModel

@{
    ViewData["Title"] = "Complete Google registration";
}

<div class="wms-auth-page">
    <div class="wms-auth-card">
        <div class="wms-auth-header">
            <div class="wms-auth-icon">WMS</div>
            <div>
                <div class="wms-auth-eyebrow">Google account connected</div>
                <h1>Complete account</h1>
                <p>Add your WMS profile details to finish sign in.</p>
            </div>
        </div>

        <div class="wms-auth-switch mb-3">
            <span>Authenticated with</span>
            <strong>@Model.ProviderDisplayName</strong>
        </div>

        <form asp-page-handler="Confirmation"
              asp-route-returnUrl="@Model.ReturnUrl"
              method="post"
              class="wms-auth-form">
            <div asp-validation-summary="ModelOnly" class="wms-auth-validation" role="alert"></div>

            <div class="form-floating wms-auth-field">
                <input asp-for="Input.Email"
                       class="form-control"
                       autocomplete="email"
                       placeholder="name@example.com" />
                <label asp-for="Input.Email">Email address</label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>

            <div class="form-floating wms-auth-field">
                <input asp-for="Input.UserName"
                       class="form-control"
                       autocomplete="username"
                       placeholder="username" />
                <label asp-for="Input.UserName">Username</label>
                <span asp-validation-for="Input.UserName" class="text-danger"></span>
            </div>

            <div class="form-floating wms-auth-field">
                <input asp-for="Input.OIB"
                       class="form-control"
                       autocomplete="off"
                       placeholder="12345678901" />
                <label asp-for="Input.OIB">OIB</label>
                <span asp-validation-for="Input.OIB" class="text-danger"></span>
            </div>

            <div class="form-floating wms-auth-field">
                <input asp-for="Input.JMBG"
                       class="form-control"
                       autocomplete="off"
                       placeholder="1234567890123" />
                <label asp-for="Input.JMBG">JMBG</label>
                <span asp-validation-for="Input.JMBG" class="text-danger"></span>
            </div>

            <button type="submit" class="wms-auth-submit">
                Finish registration
            </button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
